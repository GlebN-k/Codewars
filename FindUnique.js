function findUniq(arr) {
    let res = []

    arr.map(el => res.includes(el) ? res.push(el) : null
    //
    // let obj = {}
    // arr.forEach(el => obj[el] ? obj[el] + 1 : obj[el]=0 )
    // // {[el]:}
    return res
}
